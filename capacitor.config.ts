import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'com.edrugsearch.app',
    appName: '醫藥淘',
    webDir: 'dist',
    server: {
        androidScheme: 'https'
    }
}

export default config
